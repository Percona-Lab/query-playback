[plugin]
title=libdrizzle Client
description=This module allows queries to be executed against a MySQL or Drizzle server speaking either protocol using the libdrizzle client library.
version=0.1
static=yes
build_conditional="x${ac_cv_libdrizzle}" != "xno"
load_by_default=yes
